const User = require('./User');
/**
* Each Model consists if three properties
* @property {string} key - key used for model and database table
* @property {object} definition - sequelize model definition
* @property {object} options - sequelize model options
*/

module.exports = [User];
